function AddButton() {
  return (
    <div>
      <form>
        <label>Task: </label>
        <input placeholder="type task" />
        <button>Add to list</button>
      </form>
    </div>
  );
}

// return AddButton;

export default AddButton;
