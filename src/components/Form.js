import React, { Components } from "react";

class Form extends React.Component {
  props.name

  render() {
    return (
      <form>
        <div>
          <label>Email</label>
          <input type="text"></input>
        </div>
      </form>
    );
  }
}
export default Form;
